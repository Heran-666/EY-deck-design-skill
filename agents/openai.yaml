interface:
  display_name: "EY Deck Design"
  short_description: "Control EY deck content and PPT Master delivery"
  default_prompt: "Use $ey-deck-design to approve the Storyline and exact content, then delegate Stage 1 SVG production and Stage 2 editable-PPTX export to its embedded PPT Master."

policy:
  allow_implicit_invocation: false
