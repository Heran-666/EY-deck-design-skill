interface:
  display_name: "EY Deck Design"
  short_description: "Approve EY content and iterate PPT Master SVG options"
  default_prompt: "Use $ey-deck-design to approve the EY Storyline and exact content, generate A/B SVGs with bundled PPT Master, iterate revisions from my feedback, and publish only confirmed pages."

policy:
  allow_implicit_invocation: false
