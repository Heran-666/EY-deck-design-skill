# EY 素材引用统一与验证

更新日期：2026-09-15。按 Superpowers 测试先行、独立代码审查和完成前验证执行；结合 skill-creator 同步运行规则。

## 最小实现

- 统一候选表示：图标是 SVG 内实际矢量几何，图片是嵌入数据。审核、确认及导出不再依赖外部素材文件；受支持的同文档渐变、裁切、滤镜引用保留。
- 增加 EY 专用 `page_svg_service.py inline-resources <request>`，复用现有图标和同文档引用展开器，按原始字节嵌入已有图片。仅修改当前请求草稿，不压缩、裁切、重采样或生成页面构图；失败时不落盘，已闭合草稿不重复改写。
- 默认可读取内嵌图标库；自定义已有库通过 `--icons-dir` 显式指定。已有图片允许在草稿中使用精确路径，审核前嵌入。不新增自动搜图、图片生成、资源池同步或用户审批。
- 请求 v4 增加可选 `candidate_receipt_path`，只有内联命令要求该字段。凭据已存在的版本拒绝改写；新 Rn 使用自己的凭据路径。旧请求仍可校验/complete/export，缺少字段的未记录草稿可通过普通页面编辑内联，不得伪造凭据路径。
- complete/record、控制器及导出入口使用同一素材闭合策略。导出发现历史未闭合引用时返回上游修订和重新确认，不在导出阶段改写来源。
- 共享工具新增 EY 显式选项，沿用现有递归资源检查覆盖全部载体，展开定义内的嵌套引用，支持自定义图标完整 viewBox。默认通用行为保持。

修改 12 个现有文件，新增 EY 资源工具、资源测试及本报告，共 3 个文件。服务合同是规则主体；根 SKILL、候选流程、导出合同与集成说明同步引用边界。

## 与前两轮兼容

字号与字体规则、两份 Executor 核心参考、EY 模板、控制器文件及两层完整性检查文件未修改，并逐字与本轮开始的 Git 基线核对。

继续保留单一 A/Rn、一次服务合同执行、按反馈局部修订、集中修复、有效运行环境复用、单次来源哈希计算、平面 DrawingML 导出、文字/空格/段落审计和 postflight。资源处理在已有审核步骤内完成，不新增流程阶段。

## 验证证据

| 验证 | 结果 |
|---|---|
| 初始资源回归 | 修复前 6 项测试出现 7 个失败，复现候选接受依赖、导出缺少预检及缺少内联入口 |
| 独立审查发现 | 已记录版本保护、定义内嵌套引用、其他载体中的嵌入 SVG、非方形/非零原点图标；均补充复现并修复 |
| 最终 EY 完整回归 | 71 项通过，无跳过；包含新增 8 项资源测试 |
| 内嵌工具回归 | 106 项通过，无跳过 |
| 实际素材导出 | 内联后删除原图片仍导出成功；图标为 DrawingML custom geometry，图片为 picture，媒体保留原始图片字节，文字审计通过 |
| 历史和 Rn | 已记录 A 拒绝内联；新 Rn 可内联，原 A 和确认 SVG 字节保持 |
| 自定义图标 | 删除源图标后仍可转换，OOXML 位置和尺寸符合非零原点、非方形 viewBox 的指定框 |
| 技能与完整性 | skill-creator 校验、内嵌 attribution guard 通过 |
| 代码及文档 | 修改 Python AST、相关 Markdown 本地链接、Git 空白检查通过 |

完整回归使用本机 bundled Python，并设置相同的 `EY_DECK_SVG_PYTHON` / `EY_DECK_PPTX_PYTHON`；命令为 `-m unittest discover -s tests -q` 和 `-m unittest discover -s ppt-master/scripts/tests -q`。

验证覆盖转换与 OOXML 审计，未新增页面风格设计或逐页 PowerPoint/WPS 渲染验收。

## 生效与备份

安装目录 `/Users/hz/.codex/skills/ey-deck-design` 仍指向当前工作目录，更新直接生效。独立全局 PPT Master 未修改。

更新前文件及哈希保存在 `/Users/hz/.codex/backups/ey-deck-design/20260915-svg-resource-unification`，包含 `originals/`、`manifest.json`；更新后文件哈希另存 `after_hashes.json`。

## 后续图标表达优化（2026-09-15）

本轮按用户指定仅优化图标跨库选择和使用目的，修改服务合同、集成说明及本记录，共 3 个现有文档。

- EY 服务显式覆盖共享 README 的单一风格库选择限制：按语义选择，允许跨库，并通过轮廓、填充/描边、线宽、配色和视觉重量统一表达；品牌标识继续保留真实身份。
- 将省略装饰图标的绝对表述改为按沟通目的判断，允许识别、导航、分组、视觉节奏及强调，避免无沟通作用的堆砌。
- 明确页面 SVG 内手绘原创图标属于页面设计，不受外部素材生成禁令限制，仍遵守批准含义、模板和 SVG 技术合同。
- 按 Superpowers writing-skills 做独立场景基线与复测。基线执行者拒绝新的跨库选择和节奏图标，并对原创图标作保守解释；修改后新的独立执行者允许三种表达，并保留原有含义、视觉一致性和素材内联边界。
- skill-creator 校验、attribution guard、本轮文档链接及 Git 空白检查通过。全部 Python 文件、共享图标 README、Executor 核心、根 SKILL 和控制器与本轮前快照哈希一致；本轮未重新运行上一节的代码回归。

本轮前快照和更新后哈希保存在 `/Users/hz/.codex/backups/ey-deck-design/20260915-icon-expression-policy`，保留前一轮已完成更新。
